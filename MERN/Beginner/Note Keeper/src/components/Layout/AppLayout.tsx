import Header from "../UI/Header";
import NoteList from "../UI/NoteList";

const AppLayout = () => {
  return (
    <div>
      <Header />
      <NoteList />
    </div>
  );
};

export default AppLayout;
